const GAME_BOARD = (function() {
    let array = [
    //    0    1    2
        [' ', ' ', ' '], // 0
        [' ', ' ', ' '], // 1
        [' ', ' ', ' ']  // 2
    ]

    return {
        gameBoard
    };
})();

const createPlayer = (name) => {
    return { name }
}
